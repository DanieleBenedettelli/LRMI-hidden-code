# LEGO MINDSTORMS Robot Inventor Model Specific Blocks Map

This repository documents how to locate the Python code generated by the black-box model-specific blocks used by the LEGO MINDSTORMS Robot Inventor App hero models.

The goal is to preserve this information from obsolescence and make it easier for anyone who wants to port these blocks to pure Scratch, Pybricks, or another open runtime.

The file `LMRI.js` is provided in this repository as is. It includes the low-level python implementation of such blocks.
The file `LMRI_beautified.js` is the same code but in a more readable format.

## Context

The LEGO MINDSTORMS Robot Inventor App includes several model specific blocks for the official hero robots.

These blocks appear in projects as opcodes (with prefix `flippermodelspecific_`) such as:

```text
flippermodelspecific_bigBotCalibrate
flippermodelspecific_bigBotRemoteControl
flippermodelspecific_bigBotToggleAction
flippermodelspecific_custom
flippermodelspecific_dogBotMoveForSeconds
flippermodelspecific_dogBotSetLegsToPosition
flippermodelspecific_dogBotSetSpeed
flippermodelspecific_dogBotStartMoving
flippermodelspecific_dogBotStop
flippermodelspecific_dogBotWalkForSeconds
flippermodelspecific_moveBotCalibrate
flippermodelspecific_moveBotRemoteControl
flippermodelspecific_playBotCalibrate
flippermodelspecific_playBotDrumBarsAtTempo
flippermodelspecific_playBotRemoteControl
flippermodelspecific_sportBotRemoteControl
```

Inside the bundled JavaScript code, these opcodes are mapped to minified JavaScript functions. Those functions generate the Python code that is eventually sent to the hub.

This README provides the current opcode to JavaScript function map. Find the JS function, and then look for the micropython code after each `functionBody` tag.

## Hero Robot Map

### Charlie

| Block Opcode             | JavaScript Function | Generated Python         |
| ------------------------ | ------------------- | ------------------------ |
| `playBotDrumBarsAtTempo` | `A8e`               | `playBotDrumBarsAtTempo` |
| `playBotCalibrate`       | `O8e`               | `playBotCalibrate`       |
| `playBotRemoteControl`   | `P8e`               | `bc("A", "E")`           |

### Tricky

| Block Opcode            | JavaScript Function | Generated Python |
| ----------------------- | ------------------- | ---------------- |
| `sportBotRemoteControl` | `S8e`               | `bc("B", "A")`   |

### Blast

| Block Opcode          | JavaScript Function | Generated Python     |
| --------------------- | ------------------- | -------------------- |
| `bigBotCalibrate`     | `f8e`               | `bigBotCalibrate`    |
| `bigBotToggleAction`  | `v8e`               | `bigBotToggleAction` |
| `bigBotRemoteControl` | `C8e`               | `bc("C", "A")`       |

### MVP

| Block Opcode           | JavaScript Function | Generated Python       |
| ---------------------- | ------------------- | ---------------------- |
| `moveBotCalibrate`     | `E8e`               | `moveBotCalibrate`     |
| `moveBotRemoteControl` | `M8e`               | `moveBotRemoteControl` |

### Gelo

| Block Opcode              | JavaScript Function | Generated Python          |
| ------------------------- | ------------------- | ------------------------- |
| `dogBotWalkForSeconds`    | `g8e`               | `dogBotWalkForSeconds`    |
| `dogBotMoveForSeconds`    | `_8e`               | `dogBotMoveForSeconds`    |
| `dogBotSetLegsToPosition` | `y8e`               | `dogBotSetLegsToPosition` |
| `dogBotSetSpeed`          | `w8e`               | `dogBotSetSpeed`          |
| `dogBotStartMoving`       | `b8e`               | `dogBotStartMoving`       |
| `dogBotStop`              | `L8e`               | `dogBotStop`              |

## Full Opcode Map

```js
{
  flippermodelspecific_bigBotCalibrate: {
    jsFunction: "f8e",
    pythonFunctionBody: "bigBotCalibrate"
  },

  flippermodelspecific_bigBotRemoteControl: {
    jsFunction: "C8e",
    pythonFunctionBody: 'bc("C", "A")'
  },

  flippermodelspecific_bigBotToggleAction: {
    jsFunction: "v8e",
    pythonFunctionBody: "bigBotToggleAction"
  },

  flippermodelspecific_custom: {
    jsFunction: null,
    pythonFunctionBody: null,
    note: "Not present in extensionFunctions in the bundle"
  },

  flippermodelspecific_dogBotMoveForSeconds: {
    jsFunction: "_8e",
    pythonFunctionBody: "dogBotMoveForSeconds"
  },

  flippermodelspecific_dogBotSetLegsToPosition: {
    jsFunction: "y8e",
    pythonFunctionBody: "dogBotSetLegsToPosition"
  },

  flippermodelspecific_dogBotSetSpeed: {
    jsFunction: "w8e",
    pythonFunctionBody: "dogBotSetSpeed"
  },

  flippermodelspecific_dogBotStartMoving: {
    jsFunction: "b8e",
    pythonFunctionBody: "dogBotStartMoving"
  },

  flippermodelspecific_dogBotStop: {
    jsFunction: "L8e",
    pythonFunctionBody: "dogBotStop"
  },

  flippermodelspecific_dogBotWalkForSeconds: {
    jsFunction: "g8e",
    pythonFunctionBody: "dogBotWalkForSeconds"
  },

  flippermodelspecific_moveBotCalibrate: {
    jsFunction: "E8e",
    pythonFunctionBody: "moveBotCalibrate"
  },

  flippermodelspecific_moveBotRemoteControl: {
    jsFunction: "M8e",
    pythonFunctionBody: "moveBotRemoteControl"
  },

  flippermodelspecific_playBotCalibrate: {
    jsFunction: "O8e",
    pythonFunctionBody: "playBotCalibrate"
  },

  flippermodelspecific_playBotDrumBarsAtTempo: {
    jsFunction: "A8e",
    pythonFunctionBody: "playBotDrumBarsAtTempo"
  },

  flippermodelspecific_playBotRemoteControl: {
    jsFunction: "P8e",
    pythonFunctionBody: 'bc("A", "E")'
  },

  flippermodelspecific_sportBotRemoteControl: {
    jsFunction: "S8e",
    pythonFunctionBody: 'bc("B", "A")'
  }
}
```

## How to Use This Map

1. Search for the opcode in `LMRI.js`.
2. Locate the corresponding entry in `extensionFunctions`.
3. Follow the mapped JavaScript function.
4. Inspect the generated Python string or helper function name.
5. Reimplement the behavior in your target environment.

Possible targets include:

* Pure Scratch blocks
* Pybricks
* LRMI app python IDE
* Custom firmware tools
* Educational documentation

## Notes

Some blocks generate a direct Python helper function call, such as:

```python
dogBotWalkForSeconds(...)
```

Other blocks generate calls to shared helper functions, such as:

```python
bc("A", "E")
bc("B", "A")
bc("C", "A")
```

These shared helpers still need to be inspected and understood separately before a complete port can be written.

## Scope

This repository does not provide a complete port of the LEGO model specific blocks.

It only provides a map to help locate the relevant code inside `LMRI.js`.

`LMRI.js` is included as is, for preservation and research purposes.

## Disclaimer

LEGO and MINDSTORMS are trademarks of the LEGO Group.

This repository is an independent preservation and research effort. It is not affiliated with, authorized by, sponsored by, or endorsed by the LEGO Group.

The included `LMRI.js` file was extracted from the app DLL solely for documentation, interoperability, educational, reverse engineering, and digital preservation purposes. No malicious intent is involved.

The LEGO MINDSTORMS Robot Inventor ecosystem has been officially discontinued, and part of the motivation for this repository is to preserve technical knowledge that could otherwise be lost over time. The information provided here is intended to help hobbyists, educators, researchers, and developers understand how the model specific blocks generate Python code, and to facilitate the creation of compatible implementations for educational, archival, and interoperability purposes.

This repository is not intended to replace the original software, distribute commercial LEGO assets, bypass licensing mechanisms, or enable unauthorized use of LEGO products.

If requested by the LEGO Group, any material determined to infringe intellectual property rights will be reviewed and removed.
